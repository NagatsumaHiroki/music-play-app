# Spotify API credentials (server-side only)
# Note: Unlike Vite, these do NOT need the VITE_ prefix and are NOT exposed to the client
# The values should be the raw credentials (not base64 encoded with prefix)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
